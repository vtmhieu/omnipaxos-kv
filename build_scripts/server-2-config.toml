location = "local-2"
server_id = 2
num_clients = 1 # Hardcoded number of client connections to wait for
listen_address = "127.0.0.1"
listen_port = 8002
output_filepath = "./logs/server-2.json"

# Clock configuration (optional - defaults will be used if not specified)
[clock]
drift_rate_us_per_s = 0.0        # Clock drift rate in microseconds per second
uncertainty_us = 100             # Synchronization uncertainty bound (±ε microseconds)
sync_interval_ms = 10            # Synchronization interval in milliseconds
